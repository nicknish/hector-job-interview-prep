/**
 * In this very simple example, we have a function that adds two numbers
 * together. We're going to use this function to demonstrate how to write unit
 * tests.
 *
 * For now, add the TypeScript types to the function parameters and return type.
 * Then, open up sum.test.ts.
 */
export function sum(a, b) {
  return a + b
}
