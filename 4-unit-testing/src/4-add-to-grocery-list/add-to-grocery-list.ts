/**
 * Using TypeScript, write a function `addToGroceryList` that takes in an string
 * `item` and an array `list` and adds the item to the list. It should return
 * the amended list.
 *
 * Then, write unit tests for these scenarios:
 * - addToGroceryList('apples', []) should return ['apples']
 * - addToGroceryList('apples', ['bananas']) should return ['bananas', 'apples']
 * - addToGroceryList('apples', ['bananas']) should contain 'bananas'
 * - addToGroceryList('apples', ['bananas']) should have length 2
 *
 * Reference:
 * - https://jestjs.io/docs/using-matchers#arrays-and-iterables
 */
